Monaco workers are now bundled through Vite (`?worker`) so the editor loads in the production image, where `/monaco` public assets were never copied. Lucide is bundled via `@iconify-json/lucide` (server + client) so Iconify is not fetched at runtime after nsenter.
