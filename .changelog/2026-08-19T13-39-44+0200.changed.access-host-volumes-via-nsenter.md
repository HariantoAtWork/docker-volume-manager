Compose now starts Harbour with privileged, pid: host, and nsenter into PID 1 instead of bind-mounting the Docker socket or a workspace volume. File browsing reads `/var/lib/docker/volumes/<name>/_data` in the host mount namespace, and still falls back to VOLUME_PATH or a helper container for local `bun run dev`.
