Harbour no longer enters the host network namespace (`nsenter -n`). Bun stays in the container netns and listens on 0.0.0.0:3000 so Compose `5555:3000` reaches the Nuxt UI. Mount-only nsenter is kept for `/var/lib/docker` access.
