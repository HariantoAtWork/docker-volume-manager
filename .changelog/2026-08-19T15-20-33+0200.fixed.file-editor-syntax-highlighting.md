The volume file editor now uses CodeMirror 6 instead of Monaco, so files open with working syntax highlighting in the Docker production build. Monaco 0.56 never mounted in the Rolldown bundle, which left a blank editor.
