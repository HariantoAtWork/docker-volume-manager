The Alpine runtime image was still using apt-get, which cannot run on oven/bun:canary-alpine. Install util-linux-misc with apk so nsenter is the util-linux binary rather than BusyBox, and keep a container-namespace pause with tail so the entrypoint still works on musl/BusyBox.
