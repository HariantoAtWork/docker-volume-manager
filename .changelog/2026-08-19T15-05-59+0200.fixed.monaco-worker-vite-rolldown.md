Vite 8 Rolldown could not resolve Monaco `?worker` imports, and monaco-editor 0.56 package exports made the old `esm/vs/` worker paths resolve to missing files. Workers are now bundled with `new Worker(new URL(..., import.meta.url))` using the 0.56 export paths so production builds keep syntax highlighting.
