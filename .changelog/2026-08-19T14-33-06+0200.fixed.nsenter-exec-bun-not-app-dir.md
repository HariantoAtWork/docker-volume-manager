nsenter was executing `/proc/<pid>/root/app` (WORKDIR, a directory) because util-linux `-w DIR` does not consume DIR, which caused exit 126. The entrypoint now execs the musl loader, bun binary, and Nitro `index.mjs` from a host-visible executable rootfs after entering PID 1's mount namespace.
