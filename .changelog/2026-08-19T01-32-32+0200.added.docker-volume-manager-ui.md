Added a Harbour UI to list, inspect, create, remove and prune Docker volumes, plus browse and edit files in a bound volume. The Nitro API talks to the Engine over DOCKER_SOCKET/DOCKER_HOST, and VOLUME_BIND/VOLUME_PATH control which volume is mounted for direct file access.
